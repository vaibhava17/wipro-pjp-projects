1. CREATE TABLE dept1 ( DEPTNO INTEGER PK, ONAME VARCHAR2(30) NOT NULL, LOC VARCHAR (30) NOT NULL);

2. CREATE TABLE emp1 ( empno  NUMBER(2, 0), ename VARCHAR2(20) NOT NULL, sal NUMBER(10, 2), mgr NUMBER(2, 0) NOT NULL, deptno NUMBER(2, 0), constraints EMP1_SAL_CK CHECK (sal>5000), constraints PK_EMP1 PRIMARY KEY (empno), constraints FK_EMP1 FOREIGN KEY (empno)REFERENCES emp1 (mgr), constraints FK_DEPTNO FOREIGN KEY (deptno) REFERENCES dept1 (deptno)); 

3. CREATE TABLE Dept11 AS SELECT * FROM Dept1;
   CREATE TABLE Emp11 AS SELECT * FROM Emp1;

4. ALTER TABLE Emp1 ADD( Address VARCHAR2(30));

5. ALTER TABLE Emp1 RENAME COLUMN SAL TO SALARY;

6. ALTER TABLE emp1 DROP CONSTRAINT fk_deptno; 

7. ALTER TABLE emp1 MODIFY (ENAME VARCHAR2(40));

8. ALTER TABLE emp1 
   ALTER COLUMN ENAME VARCHAR2(30) NULL;
 
9. COMMENT ON TABLE Dept1 IS 'Depts of WIPRO';
 
10. COMMENT ON COLUMN Dept1.Deptno IS 'Deptno of WIPRO';

11. COMMENT ON TABLE Emp1 IS 'Employee of WIPRO';

12. COMMENT ON COLUMN Emp1.Empno IS 'Empno of WIPRO';

13. COMMENT ON TABLE Emp1 IS ' ';COMMENT ON COLUMN Emp1.Empno IS ' ';COMMENT ON TABLE Dept1 IS ' ';COMMENT ON COLUMN Dept1.Deptno IS ' '; 

14. AITER TABLE Emp1 set unused (Sal,Ename);

15. ALTER TABLE Emp1 DROP unused COLUMN;

16. DROP TABLE(Emp1 , Dept1);

17. CREATE TABLE Emp1 AS (SELECT * FROM Emp);

18. ALTER TABBLE Emp1 RENAME TO EMP_Test;

19. TRUNCATE TABLE EMP_Test;

20. CREATE TABLE Emp2 AS (SELECT Empno , Emane , sal FROM Emp);
 
21. DROP TABLE Emp2;

22. CREATE TABLE Emp2 AS (SELECT * FROM Emp WHERE 1=2);

23. DROP TABLE Emp2;

24. FLASHBACK TABLE Emp2 TO BEFORE DROP;
    SHOW TABLES;

25. flashback TABLE Emp2 TO before DROP rename TO Emp2_1; 

26. SELECT * FROM tab;
