# CREATE DATABASE

hive> CREATE SCHEMA NYSE;

#SHOW DATABASE

hive> SHOW DATABASES;
default
NYSE

#DROP DATABASE
hive> DROP DATABASE IF EXISTS NYSE;

#CREATE TABLE IN DATABASE
hive> CREATE TABLE IF NOT EXISTS NYSE ( Ticker string, Date Date,
Open Decimal(10,2), Open Decimal(10,2), High Deciaml(10,2), Low Decimal(10,2)
Close Decimal(10,2), Volume Int, AdjClose Decimal(10,2))
COMMENT ‘NYSE_DATA’
ROW FORMAT DELIMITED
FIELDS TERMINATED BY ‘\t’
LINES TERMINATED BY ‘\n’
STORED AS TEXTFILE, NYSE_daily.txt;

LOAD DATA LOCAL INPATH 'E:/NYSE_daily.txt'
OVERWRITE INTO TABLE records; 